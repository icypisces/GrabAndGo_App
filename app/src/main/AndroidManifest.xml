<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ntut.grabandgo">

    <!-- 兩個都是Normal Permission，不用寫程式額外跳對話框． -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_logout_register.LoginActivity" />
        <activity android:name=".login_logout_register.RegisterActivity" />
        <activity android:name=".orders_intraday.UnprocessedOrderActivity" />
        <activity android:name=".orders_intraday.CompleteOrderActivity"></activity>
    </application>

</manifest>